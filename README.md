Run it locally with `npm install` and `npm run dev`.

# Cats Playable

A tiny Pixi.js playable where cats are sorted onto matching shelves by color.

## Build

```bash
npm run build
```

The production files are generated in `dist`. 
